// Status bar implementation (placeholder)

pub struct StatusBar {
    // TODO: Add status state
}

impl StatusBar {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for StatusBar {
    fn default() -> Self {
        Self::new()
    }
}
